apiVersion: apps/v1
kind: Deployment
metadata:
  name: ssh-jumpbox-child-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ssh-jumpbox-child
  template:
    metadata:
      labels:
        app: ssh-jumpbox-child
    spec:
      containers:
        - name: app
          image: fpommerening/container-training:jumpbox
          ports:
            - name: ssh
              containerPort: 22
            - name: http
              containerPort: 5001
          volumeMounts:
            - name: ssh-conf
              mountPath: /etc/ssh/sshd_config.d
          env:
            - name: SSH_PASSWORD
              value: "MyChild"
      volumes:
        - name: ssh-conf
          configMap:
            name: ssh-jumpbox-config
      
