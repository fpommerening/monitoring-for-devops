apiVersion: v1
kind: Service
metadata:
   name: demo-apps-svc
spec:
  selector:
    app: demo-apps
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
    

    
